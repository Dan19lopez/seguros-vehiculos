/* import axios from "axios";

const baseUrl = "http://localhost:7716/vehiculos";

export const getVehiculos = async () => {
    const response = await axios.get(`${baseUrl}/vehiculos`);
    return response.data;
  };
  
  export const addVehiculo = async (vehiculo) => {
    const response = await axios.post(`${baseUrl}/vehiculos`, vehiculo);
    return response.data;
  };
  
  export const updateVehiculo = async (id, vehiculo) => {
    const response = await axios.put(`${baseUrl}/vehiculos/${id}`, vehiculo);
    return response.data;
  };
  
  export const deleteVehiculo = async (id) => {
    const response = await axios.delete(`${baseUrl}/vehiculos/${id}`);
    return response.data;
  }; */